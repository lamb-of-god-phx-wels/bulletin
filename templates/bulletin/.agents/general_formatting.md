# General Formatting

- For quotes, use ASCII double quotes (``` " ```) and not double back-ticks (``` `` ```).
- When using hyphens use a single `-` instead of `--` (i.e., don't increase the
  hyphen count) unless copying from source text which does so.