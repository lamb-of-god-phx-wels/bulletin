# The Prayers

The prayers section has the following subsections:

* Prayer of the Church
* Lord's Prayer
* Blessing


## Prayer of the Church

Title only, no subtitle or body


## Lord's Prayer

* Use the text from `assets/lords_prayer.md` verbatim for the body.
* The body is bold.
* Treat the body as a "C:" paragraph, except without the "C:" (i.e., use the
  same indentation as "C:" paragraphs).
* This subsection must not be broken across pages. Place a `\needspace{8\baselineskip}` before the `\bulletinheading` to keep the heading and body together.
* Add `\vspace{0.35em}` between the heading and the body text to provide
  adequate visual separation.


## Blessing

* Use the text from `assets/blessing.md` for the body.
* This is a responsive reading.
* Preserve new lines, but don't add extra space between paragraphs except where
  there is a blank line in the source.
